return {
	"folke/trouble.nvim",
	opts = {
		defaults = {
			entry_prefix = "@",
			prompt_prefix = "#",
		},
	}, -- for default options, refer to the configuration section for custom setup.
	cmd = "Trouble",
}
